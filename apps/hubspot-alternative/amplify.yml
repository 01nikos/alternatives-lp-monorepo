version: 1
applications:
  - appRoot: apps/hubspot-alternative
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@9.0.0
            - pnpm install
        build:
          commands:
            - cd apps/hubspot-alternative
            - pnpm run build
            - cp -r .next/static .next/standalone/apps/hubspot-alternative/.next/
            - if [ -d "public" ]; then cp -r public .next/standalone/apps/hubspot-alternative/; fi
      artifacts:
        baseDirectory: .next/standalone/apps/hubspot-alternative
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
