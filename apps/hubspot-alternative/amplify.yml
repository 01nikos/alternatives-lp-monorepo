version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm@9.0.0
        - pnpm install
    build:
      commands:
        - pnpm run build --filter=hubspot-alternative
  artifacts:
    baseDirectory: apps/hubspot-alternative/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - apps/hubspot-alternative/.next/cache/**/*
