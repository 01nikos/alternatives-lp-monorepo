version: 1
applications:
  - appRoot: apps/klaviyo-alternative
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@9.0.0
            - pnpm install
        build:
          commands:
            - cd apps/klaviyo-alternative
            - pnpm run build
            - cp -r .next/static .next/standalone/apps/klaviyo-alternative/.next/
            - if [ -d "public" ]; then cp -r public .next/standalone/apps/klaviyo-alternative/; fi
      artifacts:
        baseDirectory: .next/standalone/apps/klaviyo-alternative
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
